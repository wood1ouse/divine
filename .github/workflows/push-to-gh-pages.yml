name: Deploy to GitHub Pages

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Use Node.js
        uses: actions/setup-node@v1
        with:
          node-version: '14.x'

      - name: Install dependencies
        run: npm ci

      - name: Build app
        run: npm run build -- --prod --base-href "https://wood1ouse.github.io/divine/"
        env:
          SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
          SUPABASE_KEY: ${{ secrets.SUPABASE_KEY }}

      - name: Deploy to GitHub Pages
        uses: angular-schule/angular-cli-ghpages@1.0.6
        with:
          deploy_token: ${{ secrets.GITHUB_TOKEN }}
          project_name: dist/divine
